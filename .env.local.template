# Frontend (Vite) — local API: leave empty to use same-origin /api (proxy → port 8787), or set absolute URL.
VITE_API_BASE_URL=
VITE_DEV_USER_ID="00000000-0000-0000-0000-000000000001"
VITE_DEV_USER_EMAIL="local@localhost"

# Backend (Node `npm run dev:server`) — also set in .env.local at repo root:
# DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/cadam
# OPENAI_BASE_URL=http://127.0.0.1:8000/v1
# OPENAI_API_KEY=changeme
# OPENAI_MODEL=/model
# DEV_USER_ID=00000000-0000-0000-0000-000000000001
# DEV_USER_EMAIL=local@localhost
# MAX_TOKENS_CAP=4096
# LOCAL_SERVER_PORT=8787
