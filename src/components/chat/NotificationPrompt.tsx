interface NotificationPromptProps {
  shouldShow?: boolean;
  onDismiss?: () => void;
}

/** Disabled in local-only build. */
export function NotificationPrompt(_props: NotificationPromptProps) {
  return null;
}
